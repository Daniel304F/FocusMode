{
  "name": "focusmode",
  "version": "2.0.0",
  "private": true,
  "description": "FocusMode Browser-Extension — fachliche Tests für den Domain-Kern (src/core).",
  "type": "module",
  "scripts": {
    "test": "node --test \"tests/*.test.js\""
  }
}
